version: 2

project_name: buglens

before:
  hooks:
    - go mod tidy

builds:
  - id: buglens
    main: ./cmd/buglens
    binary: buglens
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/ai-hermes/buglens-v2/internal/version.Version={{.Version}}
      - -X github.com/ai-hermes/buglens-v2/internal/version.Commit={{.Commit}}
      - -X github.com/ai-hermes/buglens-v2/internal/version.Date={{.Date}}

archives:
  - id: default
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: git
